document.addEventListener('DOMContentLoaded',()=>{const button=document.getElementById('modeButton');if(!button)return;button.onclick=null;const apply=mode=>{document.body.classList.toggle('light',mode==='light');button.textContent=mode==='light'?'☾':'☼';button.setAttribute('aria-label',mode==='light'?'Switch to dark mode':'Switch to light mode');button.title=mode==='light'?'Dark mode':'Light mode';};let saved='dark';try{saved=localStorage.getItem('zenloth-theme')||'dark'}catch(e){}apply(saved);button.addEventListener('click',()=>{const next=document.body.classList.contains('light')?'dark':'light';apply(next);try{localStorage.setItem('zenloth-theme',next)}catch(e){}});});