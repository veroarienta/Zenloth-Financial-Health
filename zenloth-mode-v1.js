document.addEventListener('DOMContentLoaded',()=>{const button=document.getElementById('modeButton');if(!button)return;const apply=mode=>{const light=mode==='light';document.body.classList.toggle('light',light);button.setAttribute('aria-label',light?'Switch to dark mode':'Switch to light mode');button.title=light?'Dark mode':'Light mode';};let saved='dark';try{saved=localStorage.getItem('zenloth-theme')||'dark'}catch(e){}apply(saved);button.addEventListener('click',()=>{const next=document.body.classList.contains('light')?'dark':'light';apply(next);try{localStorage.setItem('zenloth-theme',next)}catch(e){}});});