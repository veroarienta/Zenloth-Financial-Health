const state={income:1200000,expenses:700000,debtPayments:150000,emergencyFund:1240000,emergencyTarget:1950000,goalProgress:28};
const $=id=>document.getElementById(id);const clamp=(n,min=0,max=100)=>Math.max(min,Math.min(max,n));const money=n=>new Intl.NumberFormat('es-CR',{style:'currency',currency:'CRC',maximumFractionDigits:0}).format(n);
const calc=()=>{const free=Math.max(0,state.income-state.expenses-state.debtPayments),savingRate=state.income?free/state.income:0,expenseRate=state.income?state.expenses/state.income:1,debtRate=state.income?state.debtPayments/state.income:1,reserve=state.emergencyTarget?state.emergencyFund/state.emergencyTarget:0;const m={cashFlow:clamp(50+savingRate*80),savings:clamp(50+reserve*50),spending:clamp(100-expenseRate*75),debt:clamp(100-debtRate*120),goals:clamp(40+state.goalProgress*.6)};return{free,savingRate,expenseRate,debtRate,reserve,m,score:Math.round(m.cashFlow*.25+m.savings*.25+m.spending*.2+m.debt*.15+m.goals*.15)}};
const metric=(n,v)=>`<article class="card metric"><div class="metric-top"><span class="metric-name">${n}</span><strong class="metric-score">${Math.round(v)}</strong></div><div class="metric-bar"><i style="width:${v}%"></i></div></article>`;
function render(){const c=calc(),pct=Math.round(c.reserve*100),status=c.score>=80?'Good':c.score>=65?'Fair':'Needs attention';$('dashboard').innerHTML=`<section class="hero-grid"><article class="card score-card"><div class="ring"><div class="ring-inner"><strong>${c.score}</strong><span>${status}</span></div></div><div class="score-copy"><div class="eyebrow">Your financial health</div><h2>Your finances are looking ${status.toLowerCase()}.</h2><div class="delta">↑ ${Math.max(1,Math.round(c.score-76))} points <span>vs. last month</span></div></div></article><article class="card action-card"><div class="eyebrow">Your next best action</div><h2>${c.reserve<1?'Build your emergency fund':'Accelerate your next goal'}</h2><p>${c.reserve<1?`You're ${pct}% of the way to your target. Protecting this reserve is your highest-priority action.`:'Your emergency fund is covered. Direct part of your monthly margin toward your next priority.'}</p><button class="btn" id="viewPlan">View plan →</button></article></section><section><div class="section-head"><h3>What makes up your score</h3><span>Updated today</span></div><div class="metrics">${metric('Cash Flow',c.m.cashFlow)}${metric('Savings',c.m.savings)}${metric('Spending',c.m.spending)}${metric('Debt',c.m.debt)}${metric('Goals',c.m.goals)}</div></section><section class="two-col"><article class="card section-card"><div class="section-head"><h3>Why your score changed</h3><span>This month</span></div><div class="insights"><div class="insight"><span class="tag">Positive</span><h4>${c.savingRate>=.2?'Strong monthly margin':'Room to improve savings'}</h4><p>You have ${money(c.free)} available after monthly expenses and debt payments.</p></div><div class="insight"><span class="tag">${c.reserve>=.6?'On track':'Watch'}</span><h4>Emergency fund</h4><p>${pct}% funded. Increasing this reserve improves your financial resilience.</p></div><div class="insight"><span class="tag ${c.expenseRate>.6?'watch':''}">${c.expenseRate<=.6?'Positive':'Watch'}</span><h4>Spending load</h4><p>Monthly expenses represent ${Math.round(c.expenseRate*100)}% of income.</p></div><div class="insight"><span class="tag">${c.debtRate<=.2?'On track':'Watch'}</span><h4>Debt load</h4><p>Required debt payments represent ${Math.round(c.debtRate*100)}% of income.</p></div></div></article><article class="card section-card"><div class="section-head"><h3>Financial Health trend</h3><span>Last 6 months</span></div><div class="trend">${[46,53,58,67,72,c.score].map((v,i)=>`<div class="trend-col"><i style="--h:${v}%"></i>${['May','Jun','Jul','Aug','Sep','Oct'][i]}</div>`).join('')}</div></article></section><section class="card section-card"><div class="section-head"><h3>Goals</h3><span>Progress toward what matters</span></div><div class="next-goals"><div class="goal-row"><div class="goal-icon">◎</div><div class="goal-body"><strong>Emergency fund</strong><span>${money(state.emergencyFund)} of ${money(state.emergencyTarget)} saved</span></div><div class="goal-value"><strong>${pct}%</strong><span>progress</span></div></div><div class="goal-row"><div class="goal-icon">⌂</div><div class="goal-body"><strong>Home down payment</strong><span>₡2.8M of ₡10M saved</span></div><div class="goal-value"><strong>${state.goalProgress}%</strong><span>progress</span></div></div></div></section><section class="card section-card data-panel"><div class="section-head"><h3>Financial data</h3><span>Demo inputs</span></div><div class="data-grid"><label>Monthly income<input id="income" type="number" value="${state.income}"></label><label>Monthly expenses<input id="expenses" type="number" value="${state.expenses}"></label><label>Debt payments<input id="debtPayments" type="number" value="${state.debtPayments}"></label><label>Emergency fund<input id="emergencyFund" type="number" value="${state.emergencyFund}"></label></div><button class="btn" id="updateHealth">Update Financial Health</button></section>`;$('viewPlan').onclick=()=>detail('goals');$('updateHealth').onclick=()=>{['income','expenses','debtPayments','emergencyFund'].forEach(k=>state[k]=Number($(k).value)||0);render()}}
function detail(s){const c=calc(),map={cashflow:['Cash Flow','Cash flow is your available margin.','You currently have '+money(c.free)+' left after monthly expenses and debt payments.'],savings:['Savings','Savings is building your financial resilience.',Math.round(c.reserve*100)+'% of your emergency-fund target is currently funded.'],spending:['Spending','Spending load is part of your daily financial health.','Monthly expenses are '+Math.round(c.expenseRate*100)+'% of income.'],debt:['Debt','Debt load is measured against income.','Required debt payments are '+Math.round(c.debtRate*100)+'% of income.'],goals:['Goals','Your goals turn financial health into action.','Emergency savings is the current priority.']};const x=map[s]||map.goals;$('details').classList.remove('hidden');$('details').innerHTML=`<article class="card"><div class="eyebrow">Financial Health · ${x[0]}</div><h2 style="margin:8px 0 6px;font-size:23px">${x[1]}</h2><p style="color:var(--muted);line-height:1.6;font-size:13px;max-width:640px">${x[2]}</p><button class="btn" id="back">← Back to overview</button></article>`;$('back').onclick=()=>{$('details').classList.add('hidden');window.scrollTo({top:0,behavior:'smooth'})};$('details').scrollIntoView({behavior:'smooth'})}
document.querySelectorAll('.bottom-item').forEach(b=>b.onclick=()=>{document.querySelectorAll('.bottom-item').forEach(x=>x.classList.remove('active'));b.classList.add('active');b.dataset.section==='overview'?($('details').classList.add('hidden'),window.scrollTo({top:0,behavior:'smooth'})):detail(b.dataset.section)});$('modeButton').onclick=()=>{document.body.classList.toggle('dark');$('modeButton').textContent=document.body.classList.contains('dark')?'☀':'◐'};
const style=document.createElement('style');style.textContent='.data-panel{margin-bottom:100px}.data-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin:16px 0}.data-grid label{display:flex;flex-direction:column;gap:7px;font-size:12px;color:var(--muted)}.data-grid input{padding:12px;border:1px solid var(--line);border-radius:12px;background:var(--surface);color:var(--text);font:inherit}@media(max-width:650px){.data-grid{grid-template-columns:1fr}}';document.head.appendChild(style);render();